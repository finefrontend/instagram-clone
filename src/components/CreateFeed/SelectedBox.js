import React from 'react';
import styled from 'styled-components';

const SelectedBox = () => {
  return <SelectedBoxWrapper></SelectedBoxWrapper>;
};

const SelectedBoxWrapper = styled.section`
  width: 100%;
  aspect-ratio: 1 / 1;
  background-color: lightgray;
`;

export default SelectedBox;
